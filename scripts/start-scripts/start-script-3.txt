# Проверка структуры корня и основных каталогов
ls /

# Навигация по дереву VFS (абсолютные и относительные пути)
cd /home
ls
cd user
ls

# Просмотр документации из каталога /docs
cd /docs
ls
cat manual.txt

# Просмотр конфигурации из /etc
cd /etc
ls
cat config.json

# Возврат в домашний каталог пользователя и работа с файлами
cd /home/user
ls

# Создание нового пустого файла (демонстрация touch)
touch notes.txt
ls
cat notes.txt

# Проверка команды echo (вывод строки в стандартный вывод)
echo "Проверка вывода текста через echo"

# Демонстрация uniq на файле с повторяющимися строками
# Предполагается, что в /home/user/readme.txt есть две одинаковые соседние строки
cat readme.txt
uniq readme.txt

# Информация о виртуальной системе
uname

# Сохранение текущего состояния виртуальной файловой системы (с учётом созданного файла)
vfs-save